#[cfg(test)]
mod tests {
    use super::super::s_/*{{ id }}*/_/*{{ index}}*/;
    use crate::utils::{json::ValueExtension, test::t};

    #[test]
    fn test() -> () {
        t(
            module_path!(), //
            &vec![s_/*{{ id }}*/_/*{{ index}}*/::SOLUTION],
            |solve, input| {
                serde_json::Value::from(solve(
                    // input[0].as_str().unwrap().to_string(),
                    // input[1].as_f64().unwrap(),
                    // input[2].to_i32().unwrap(),
                    // input[3].to_i32_array().unwrap(),
                    // input[4].to_i32_matrix().unwrap(),
                    // ...
                ))
            },
        );
    }
}
